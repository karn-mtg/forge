/**
 * Built-in card decorators (standalone overlay pills).
 *
 * NOTE: EDHREC is now a unified widget — it owns both its panel and its card
 * badges. It is registered in builtins.ts via WidgetDef.decorator, not here.
 * This file is kept for future standalone decorators that have no panel.
 */

/** No-op — kept for import-side-effect compatibility. */
export function registerBuiltinOverlays(): void { /* intentionally empty */ }
